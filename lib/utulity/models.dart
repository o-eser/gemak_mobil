class PicSlideModel {
  final String pic, picTitle, picLink;

  PicSlideModel(this.pic, this.picTitle, this.picLink);
}


class SecondSlideModel {
  final String pic, sPic, title, picLink, description;

  SecondSlideModel(
      this.pic, this.sPic, this.title, this.picLink, this.description);
}

class NewsModel{
  final String pic, title, description, date;

  NewsModel(this.pic, this.title, this.description, this.date);
}
class SatisModel{
  final String link , pic,   buttonTitle,   buttonLink, description;

  SatisModel(this.link, this.pic, this.buttonTitle, this.buttonLink, this.description);
}

